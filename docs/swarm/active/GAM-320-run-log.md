# GAM-320 run log

Issue: https://linear.app/gamitch/issue/GAM-320/arrowdown-opens-the-next-up-rows-aria-disabled-moremenu-mid-write
Tier label on issue: `fast` (not `tier/unreviewed`, so no tiering-at-claim judgement call needed — item 28d does not apply).

- 2026-08-12: Claimed GAM-320. Moved Todo → In Progress via Linear GraphQL API, then re-read the issue fresh (not the mutation response) and confirmed `state.name == "In Progress"`. Read AGENTS.md § "Where work comes from" and constitution items 28, 26, 19 first, per dispatch instructions.
- 2026-08-12: Premise re-verified directly against `node_modules/@astryxdesign/core` (Button.js:355/372-380, DropdownMenu.js:264-272, MoreMenu.js:57-72) and `docs/swarm/astryx-api.md:4822` (MoreMenu.isDisabled) / `:1884` (per-entry `DropdownMenuOption.isDisabled`) — issue's claims hold verbatim on current `main` (GAM-304 merged; line numbers in the issue body were for the pre-merge branch and have shifted slightly in `StudentHome.tsx`, content unchanged).
- 2026-08-12: Tier decision — FAST (orchestrator implements directly), per constitution item 26: no write path, no schema/RLS/auth change, no exported-signature change, the diff is ~15 production lines, and a named mutation exists. No subagent dispatched for this row — FAST tier requires none, so the "dispatch/verdict" pairing and the background-subagent hazard this run was warned about do not apply here.
- 2026-08-12: Implementation choice — swapped the guard from `MoreMenu`-level `isDisabled` (blocked the trigger, which fixed mouse but left ArrowDown able to open the menu per Button.js's Enter/Space-only keydown suppression) to per-entry `DropdownMenuOption.isDisabled` on the "Can't go" item (documented at astryx-api.md:1884). This makes mouse and keyboard agree: the menu opens for both, and the guarded action no-ops for both. This is the primary fix the issue itself named (not the "file upstream and leave code" alternative) — chosen because it is the issue's own scoped resolution, does not reduce keyboard operability, and the write path stays doubly guarded (`DropdownMenuItem`'s own `isDisabled` check plus `handleRsvpChange`'s pre-existing `isRsvpSubmitting` early-return).
- 2026-08-12: This reverses part of a previously-passed GAM-304 test's specific assertion (trigger-level `aria-disabled`), because that assertion was testing the exact behavior GAM-320 reports as the defect. Updated the same test (`StudentHome.test.tsx`, "sibling half" describe block) in the same commit rather than leaving it red, per Definition of Ready item 5 ("any reversal of previously-passed work is explicit"). Stated and defended here and in the PR body per item 26.
- 2026-08-12: `npx tsc --noEmit` clean after the source edit.
- 2026-08-12: Committed the fix (`5c57e4e`) before mutating, per item 26's fast-tier working rule. Mutation: reverted `StudentHome.tsx` to the old trigger-level `isDisabled` in the working tree (test file left at HEAD, i.e. the new GAM-320 assertions). `npx vitest run StudentHome.test.tsx -t "sibling half"` went RED: `expected 'true' to be null` on the trigger's `aria-disabled` check — proving the updated test actually catches the regression this issue reports. Reverted the mutation with `git checkout -- src/pages/home/StudentHome.tsx`, re-ran the same test: GREEN.
- 2026-08-12: Baseline derived at merge-base `ff8fd92` (== `origin/main`, via a disposable `git worktree` + `npm ci`, then removed): `npx vitest run` → 93 files / 2407 tests; `npx vitest run src/pages/home/` → 4 files / 227 tests.
- 2026-08-12: Gate run (`gate-run` skill, commit `50fe547`, tree clean, `--require-clean`): tsc PASS · vite build PASS · format:check PASS · eslint PASS (0 errors, 377 pre-existing warnings) · vitest full PASS 2407/2407 (baseline 2407, +0) · vitest scoped PASS 227/227 (baseline 227, +0). VERDICT: PASS — all six gates.
- 2026-08-12: If this line is the last one in this file, the run died after gates passed but before the PR was opened — the branch `claude/gam-320-arrowdown-moremenu-disabled` at `50fe547` (plus any commit after it) holds the finished, gated fix; only the PR and the Linear move to `In Review` would remain.
