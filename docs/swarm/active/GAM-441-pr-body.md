Closes GAM-441

**Draft — opened early on purpose.** AGENTS.md wall 3: the PR credential this run
holds expires at `2026-08-21T06:02:24Z` (decoded from the live token, not guessed),
so the PR is opened at minute ~4 and pushed into, rather than deferred to the end.
Body is finalized before the draft flag clears.

## What this is

The PRD still mandates the table-based `/meetings` page, so under constitution
item 1 (PRD requirement IDs outrank everything below) no packet describing series
cards can pass `checker-premise`. This PR is the paperwork that unblocks the
eleven sibling `meetings-redesign` tickets: amend MTG-01 and the §7.1 route row,
record the owner rulings, commit the binding reference figures, and author the
`meetings-design` skill.

## Tier

`tier/fast`, stated and defended in `docs/swarm/active/GAM-441-run-log.md` as item
26 requires. Summary: zero lines of production change, and every Allowed File is a
protected orchestrator-owned path that AGENTS.md forbids workers and checkers to
edit — so STANDARD's and HEAVY's worker leg cannot lawfully be executed here. An
independent read-only `checker-premise` round is added on top, because this
artifact is the premise eleven downstream packets will be graded against.

## Status

Work in progress. See the run log for the live state.
