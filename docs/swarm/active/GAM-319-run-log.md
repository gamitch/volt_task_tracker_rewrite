# GAM-319 run log

Issue: https://linear.app/gamitch/issue/GAM-319/a-failed-rsvp-save-tells-the-user-couldnt-load-this-data-read-copy-on
Title: A failed RSVP save tells the user "Couldn't load this data" — read copy on a write path
Tier (as filed): `tier/fast`, labels `fast`, `w5`

- [x] Claimed: moved GAM-319 `Todo → In Progress` via direct GraphQL (`issueUpdate`), then re-read the issue and confirmed `state.name == "In Progress"`. If this line is the last one in this file, the run died right after claiming, before any premise check or work happened.
- [x] Premise re-verified at current `main` (GAM-304 merged via PR #143 / `385df28`, per the issue's own explicit warning to re-check after that merge). Confirmed on disk, not from the issue text: `extractRsvpErrorMessage` now lives at `StudentHome.tsx:922` (was 963 pre-merge) and `ParentHome.tsx:1017` (unchanged); both still forward `error.message` unconditionally for any `isSupabaseLoaderError` value. `loader.ts`'s `toLoaderError` (loader.ts:116-121) still sets `message: DEFAULT_LOADER_ERROR_MESSAGE` ("Couldn't load this data...") for every rejection except a `SupabaseNotConfiguredError`, whose `message` it reuses verbatim — so the bug as described is real on `main` today, not just on the old branch. Fix mechanism identified: `toLoaderError` always sets `cause: raw`, and `raw instanceof SupabaseNotConfiguredError` is exactly the passthrough condition — so `error.cause instanceof SupabaseNotConfiguredError` (checked at the two call sites, no `loader.ts` change) discriminates cleanly without touching the exported `SupabaseLoaderError` shape. `functions.ts:76` already establishes the house style for this exact rewording ("reworded for an action rather than a load"), used as the model for the new copy. `extractRsvpErrorMessage` is not imported by any other module (grepped) — confirms FAST tier is safe (item 26: no signature another module imports). If this line is the last one in this file, the run died after confirming the premise but before (or during) the FAST-tier implementation.
- [x] Tier: FAST (per the issue's own suggested tier and item 26 — two files, display strings only, no write path, ≤20 lines, named mutation exists). Orchestrator (me) implemented directly, no packet/worker/checker.
- [x] Fix implemented and pushed (`5081b1e`): `extractRsvpErrorMessage` in `StudentHome.tsx`/`ParentHome.tsx` now checks `error.cause instanceof SupabaseNotConfiguredError` and only passes the loader message through for that one case; every other `isSupabaseLoaderError` rejection now gets a new write-flavoured `RSVP_SAVE_ERROR_MESSAGE` instead of `loader.ts`'s read-flavoured default. Added one new test per file (GAM-319 describe blocks) asserting the write-flavoured copy renders and the read-flavoured string does not, for a plain `SupabaseLoaderError`-shaped rejection that is not `SupabaseNotConfiguredError`. `npx vitest run src/pages/home/StudentHome.test.tsx src/pages/home/ParentHome.test.tsx`: 121/121 passed. If this line is the last one in this file, the run died after the fix landed but before the mutation-replay proof or the gates.
- [x] Mutation replay (named mutation from item 26's FAST rule): committed the fix first (`5081b1e`), then reverted both call sites in-place to the old unconditional `if (isSupabaseLoaderError(error)) return error.message;` passthrough (source only, tests untouched) and re-ran the same scoped vitest command. Both new GAM-319 tests went red, reproducing the exact defect text: StudentHome rendered `"Couldn't save your RSVPCouldn't load this data. Check your connection and try again."`, ParentHome rendered `"Couldn't save this RSVPCouldn't load this data. Check your connection and try again."` — the same concatenation shape `checker-reviewer` originally caught on GAM-304. Then `git checkout -- src/pages/home/StudentHome.tsx src/pages/home/ParentHome.tsx` to restore the fix and re-ran: 121/121 green again. If this line is the last one in this file, the run died between the mutation proof and running the full gate suite.
