# GAM-371 run log

- 2026-08-14T00:00:00Z — Dispatched from Linear (Todo). Starting claim sequence per constitution item 28c: move Todo -> In Progress, then re-read to confirm. If this line is the last one in this file, the run died before confirming the claim.
- 2026-08-14T00:05:00Z — Claimed. `issueUpdate` to In Progress returned `success: true`; read-back via a fresh `issue(id:...)` query confirms `state.name == "In Progress"`. Labels are `fast`, `Improvement` — tier/fast, not tier/unreviewed, so no tiering judgement is required before this claim. Issue title/body fetched live from Linear (not from this dispatch prompt).
- 2026-08-14T00:10:00Z — Premise check (item 26 FAST, no packet needed but the issue body itself disclaims the environment): issue text claims "This cannot be run in a dispatched container as things stand" because `playwright` is absent from `node_modules`/`package.json`. Measured directly in this container: `npm install --no-save playwright` (2 packages, no package.json change — verified via `git status --porcelain` empty afterward) then `npx playwright install --with-deps chromium` both succeeded (chromium 151.0.7922.34 downloaded, apt deps installed). Ran `npm run build` (green) then `npx playwright test tests/e2e/public-routes.spec.ts --project=mobile-light`: the `/login` overflow test at line 62 passes; 3 pre-existing `/accept-invite` failures are unrelated (missing invite token param) and out of scope. **Conclusion: the disclosed environment constraint is stale in this specific dispatched container — Playwright installs and runs here.** Tier stays FAST per the issue's own item 26 analysis. Proceeding to implement: add a 320px-wide viewport project to `playwright.config.ts` and prove the mutation (revert GAM-271's fix, confirm the new project reddens at 320, restore, confirm green).
- 2026-08-14T00:20:00Z — **Process correction.** The three commits above ("start run log", "record claim confirmation", "record premise re-check") were pushed directly to `main`, not a `claude/gam-371-...` branch -- an error; I never created the feature branch before the first commit. Corrected now: created `claude/gam-371-e2e-narrow-viewport`, rebased the two unpushed local commits (`playwright.config.ts` change + this log) onto `origin/main`, reset local `main` back to `origin/main`, and pushed the branch. All further commits for GAM-371 go on this branch only. While rebasing, `git log origin/main` showed PR #183 (GAM-271) had just merged -- so the "major discovery" below, while true at the moment it was measured, is now stale in the opposite direction: **GAM-271's fix is on `main` as of this rebase**, so the new `mobile-narrow-320` project should be green on this branch, not red. Re-verifying below rather than trusting that inference.
- 2026-08-14T00:15:00Z — **Major discovery, recorded before proceeding further.** Implemented the fix (added `mobile-narrow-320` project, viewport 320x568, to `playwright.config.ts`) and ran it against unmodified `main` to prove redness. It failed as expected -- but the failure numbers (`scrollWidth` 360 vs `clientWidth` 320, i.e. exactly the 40px overflow GAM-271's own issue text cites as the *pre-fix* measurement) revealed that **GAM-271's fix is not actually on `main`.** Verified: `git merge-base --is-ancestor 7a9d9da HEAD` -> not an ancestor; the fix commit lives only on `origin/claude/gam-271-login-card-overflow`, 14 commits ahead of `main`, per that branch's own run log (`docs/swarm/active/GAM-271-run-log.md`, 2026-08-14 06:38Z entry): `gh pr create` returned `HTTP 401 Bad credentials`, so the PR was never opened and the issue sits `In Review` with its fix unmerged. GAM-371's own "Verification note" ("re-checked against `main`... confirmed green both before and after GAM-271's fix") is true of the *spec's blindness* claim but does not claim the fix is merged, and it is not. **Net effect: the actual login-card overflow bug GAM-271 was filed to fix is still live in production on `main` today.** This is out of GAM-371's declared scope ("No production code") and not something I have authorization to merge unilaterally under a FAST-tier dispatch for a different issue. Filing a Linear escalation for GAM-271 to flag its fix is ready-but-unmerged, and proceeding with GAM-371 exactly as scoped: ship the new coverage only, let its current-on-`main` failure stand as the mutation-proof (verified separately, see below, that cherry-picking GAM-271's fix commit 7a9d9da into a throwaway local commit turns it green, then reverted with `git reset --hard` back to the config-only commit -- LoginPage.tsx is untouched by this PR). `tests/e2e/**` is not wired into any GitHub Actions workflow (grepped `.github/workflows/`, no matches for `playwright`/`e2e`), so this expected-until-GAM-271-merges failure does not block CI or any other PR's six gates.
