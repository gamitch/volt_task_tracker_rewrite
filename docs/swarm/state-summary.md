# Swarm State Summary

## Current Phase
E1 (Scaffold + theme) and E2 (Schema + RLS + metric views) are now fully done through T013, and E3 has opened up. T001, T002, T002a, T003, T004, T005, T009, T010, T011, T012, T013, T015 complete — E1's CI pipeline is in place, E2's full schema+RLS+metric-view layer (identity/roster, scheduling/attendance, support+audit-triggers, RLS helpers+policies on all 14 tables, and the three PRD 8.4 metric views) is migrated and Passed, and E3's Auth provider config is authored/staged (pending George's real Google OAuth client for end-to-end verification). T006 (AppShell + TopNav), T014 (NFR-03 metric-view fixture tests), T017 (send-invite Edge Function), T032 (checkin Edge Function), and T061 (Schema verification + mapping doc copy) are all Ready and undispatched. Nothing is currently In Progress — see Active section below (empty). Agent dispatch capacity has resumed since the earlier session-limit pause (see "Session-limit interruption" note below for what happened during that pause). Full breakdown in `docs/swarm/task-ledger.md` (71 tasks, T001–T070 + T002a, epics E1–E11).

**Session-limit interruption (2026-07-17, handled directly by the orchestrating session, not a sub-agent):** two agent dispatches failed mid-run with "You've hit your session limit": (1) T005's worker-implementer was cut off while deleting a self-declared temporary test file (`src/app/_scratch.guards.manualcheck.test.tsx` — its own header comment stated it was "NOT part of the final artifact, deleted before the worker packet is handed back"); the orchestrating session completed that already-declared deletion directly (not a judgment call, just finishing a stated action) and confirmed `npm run build`/`typecheck` pass clean, lint has only pre-existing non-blocking fast-refresh warnings. (2) T010's close-out (foreman-planner) failed before writing anything — no partial/inconsistent state resulted; the orchestrating session performed that close-out directly instead, since it already held the exact, fully-specified instructions it had just given the failed agent (mechanical bookkeeping, not a new judgment call). T003 and T005 remain unchecked and their close-outs are NOT yet done — those still need real checker-tests/checker-reviewer agent runs once the session limit clears; nothing was rubber-stamped.

## Completed
- **T001** — Vite + TS(strict) + ESLint/Prettier scaffold. Passed checker-tests: build/typecheck/lint/format:check all clean, strict mode confirmed, zero Tailwind/shadcn, scope exceptions (index.html, package-lock.json) verified per D001.
- **T002** — Astryx install + `volt.ts` theme. Passed checker-accessibility (attempt 1 FAIL was an upstream type-gap issue, not worker fault; attempt 2 PASS): `volt.ts` verbatim vs DES-03, `astryx-augment.d.ts` fixes the upstream type gap and was empirically verified via negative-control test, build/typecheck clean, WCAG AA contrast passes both modes.
- **T002a** — React 18→19 upgrade (D002 corrective task). Passed checker-tests (attempt 1 FAIL was a legitimate but narrow pre-existing `format:check` gap traced to T002, not a T002a worker fault; attempt 2 PASS on merits): react/react-dom at 19.2.7, `npm ls` clean, `react.use()` confirmed a function, build/typecheck/lint/vitest all exit 0, mandatory runtime smoke test genuinely renders Theme+App, `format:check` fixed via a `package.json` glob negation scoped to exactly `src/theme/volt.ts` (independently verified not over-broad), `volt.ts` still byte-identical to DES-03. D002's stack-lock reversal is now fully closed out end-to-end.
- **T009** — Migration: identity/roster tables. Passed checker-tests (1st attempt, MINOR finding, non-blocking): all 5 tables (profiles, teams, seasons, students, guardian_links) verified column-by-column against PRD 8.1, zero deltas; id/created_at/FK-restrict conventions and the `seasons` partial unique index correct; no RLS present (correct, out of scope). MINOR: `profiles.avatar_url text not null` with no default is a genuine PRD 8.1 ambiguity (SET-01 implies avatar upload happens post-creation) that would block `profiles` inserts — amended onto T019's acceptance criteria (T019 is the first task to insert into `profiles`), not spun out as a new task. See `docs/swarm/verification-log.md` T009 entry and task-ledger.md T019 detail block.
- **T010** — Migration: scheduling/attendance tables. Passed checker-tests (1st attempt, MINOR finding, non-blocking): all 5 tables (invites, events, event_sessions, rsvps, attendance) verified column-by-column against PRD 8.1, zero deltas; FK cascade/restrict scoping, role_enum reuse, and both unique constraints correct; T009's file confirmed untouched. MINOR: `event_sessions.notes text not null` with no default — same pattern as T009's `avatar_url`, but with stronger evidence (MTG-02 and OUT-02 both imply notes is optional) — amended onto T031 and T039's acceptance criteria, not a new task. See `docs/swarm/verification-log.md` T010 entry and task-ledger.md T031/T039 detail blocks.
- **T003** — CSS cascade layers + `theme.css` build pattern. Passed checker-tests (1st attempt, clean, no findings): build/typecheck/lint/format:check all exit 0; `theme.css` declares `@layer reset, astryx-base, app;` first, matching NFR-08 exactly, with every rule confirmed inside a layer block; Astryx's own reset/base CSS confirmed pre-wrapped, not something the worker needed to wrap; `volt.ts` untouched (still byte-identical to DES-03); `main.tsx` diff exactly two lines; `dist/assets/theme.css` confirmed a real static file linked via `<link>` (DES-07, no runtime injection); no forbidden-file violations. See `docs/swarm/verification-log.md` T003 entry.
- **T005** — Router skeleton + route guards + deep-link redirect. Passed checker-reviewer (attempt 1 FAIL was a legitimate BLOCKER-class finding, not a false alarm — `/kiosk/:sessionId` had been left as a fully public, unguarded route against PRD Section 7's explicit coach/admin assignment and SEC-04's "no public pages" rule; attempt 2 PASS on merits): kiosk route now genuinely wrapped in RequireAuth+RequireRole(['coach','admin']) in the real JSX tree, Role union extended with 'coach', all 13 Section 7 routes still present, main.tsx/App.tsx still correctly unwired (T006's job), build/typecheck/lint clean, no forbidden-file violations. **Deferred follow-ups logged for whichever future task next touches `guards.tsx`/routing (expected T006):** (a) K2 — the `Role` union is only minimally patched with 'coach'; full reconciliation against the PRD's complete role vocabulary (admin/coach/student/parent per AUTH-05) is still open; (b) K3 — `RequireRole` calls `pushToast` during render rather than in an effect; deliberately left untouched through both T005 attempts, deferred to when real toast UI lands. Neither is a new task — just context T006 (or whichever task owns this code next) should be aware of. See `docs/swarm/verification-log.md` T005 entry.
- **T011** — Migration: support tables + audit triggers (DATA-02). Passed checker-tests (1st attempt, MINOR finding, non-blocking): all 4 support tables (notification_prefs, calendar_feeds, email_log, audit_log) verified column-by-column against PRD 8.1, zero deltas; role_enum reused not redefined; all 5 DATA-02 triggers independently tested against a real scratch Postgres instance with full positive AND negative controls, all 12 sub-tests passed; T009/T010 confirmed zero diff; all 5 meta jsonb payloads confirmed no-PII (IDs/enum/booleans only). MINOR: notification_prefs defaults all 6 EML-02 category bools to true, not just digest_enabled — checker judged this a reasonable opt-in-by-default interpretation and recommended accepting as-is (no follow-up task). Design decision on `audit_log.actor` NOT NULL ruled acceptable but logged as a standing operational risk (see Known Decisions below) rather than amended onto specific downstream tasks. See `docs/swarm/verification-log.md` T011 entry.
- **T004** — CI pipeline (typecheck/lint/unit/build + bundle budget). Passed checker-tests (1st attempt, clean, no defect findings): `.github/workflows/ci.yml` structurally correct (push/pull_request triggers, Node 20.18.1 pinned, `npm ci`, five genuinely real-fail steps, no error-suppression anywhere); `package.json` confirmed zero diff; checker independently re-ran typecheck/lint/test/build commands extracted verbatim from the YAML rather than trusting worker claims; checker ran its own full bundle-size inflate/revert cycle — clean build 60236 bytes gzip vs 307200 byte budget passes, checker's own inflated build (334497 bytes, via a different injection method than the worker's 414185) correctly fails, fully reverted and reconfirmed clean; NFR-04 scoping (eager entry chunks only, not total `dist/`) confirmed correct and forward-compatible with future route-level splitting; YAML validated via PyYAML; deliverable location confirmed correct. MINOR finding was a checker-calibration non-issue, not a task defect — see the standing process note below. See `docs/swarm/verification-log.md` T004 entry.
- **T013** — Metric views (verbatim PRD 8.4). Passed checker-tests (1st attempt, clean, no findings): all three views (`v_student_hours`, `v_student_participation`, `v_team_participation`) confirmed byte-verbatim vs. PRD 8.4 ground truth via SHA-256 checksum, not eyeballed; zero formula duplication in TypeScript (grep swept `src/`); all 4 NFR-03 fixture cases (excused-shrinks-denominator, hours_override-wins, check-in clamping both partial and fully-outside-window, no-completed-sessions-produces-zero-rows) independently re-validated on a fresh scratch Postgres using the checker's own independently-designed fixtures, not the worker's. Null-`team_ids` "applies to all teams" semantics re-confirmed against a 5-student/4-team fixture. T009–T012 confirmed unchanged; zero PII columns in any view, no self-referential `students` subquery — correctly leaves the Trap 1 teammate-leaderboard-visibility gap deferred to a future task (most likely T044), not a T013 defect. No forbidden-file violations. See `docs/swarm/verification-log.md` T013 entry and `docs/swarm/archive/T013-worker-packet.md` / `docs/swarm/archive/T013-checker-packet.md` for full packets. T014 (NFR-03 fixture tests) unblocked (Blocked→Ready) as a direct result.
- **T015** — Supabase Auth provider config (`supabase/config.toml` new + `.env.example` extended). Passed checker-tests (1st attempt, NIT only, no rework): `enable_signup = false` independently confirmed at both `[auth]` and `[auth.email]` levels (AUTH-01); `[auth.external.google]` uses `env(SUPABASE_AUTH_EXTERNAL_GOOGLE_CLIENT_ID)`/`env(SUPABASE_AUTH_EXTERNAL_GOOGLE_SECRET)` references only, zero literal secrets across three independent grep patterns. Worker's env-var naming (`..._EXTERNAL_GOOGLE_...`, following the `auth.external.google` section path) deviated from the packet's illustrative example; Supabase CLI wasn't available in the checker's sandbox to reproduce the worker's claimed CLI validation, so the checker did a manual structural review instead and judged the naming logically sound and within the packet's explicit allowance — passed with the caveat noted rather than treated as a gap. `.env.example` confirmed blank-placeholder-only; `.env` confirmed not created and correctly gitignored. Worker's unrequested `minimum_password_length = 8` (CLI default 6) given an explicit NIT verdict — reasonable in-scope hardening, not a defect. D001-method forbidden-file check clean. External blocker (George's real Google OAuth client doesn't exist yet) correctly handled: config is staged/functional-once-credentialed, nothing faked. See `docs/swarm/verification-log.md` T015 entry and `docs/swarm/archive/T015-worker-packet.md` / `docs/swarm/archive/T015-checker-packet.md` for full packets.
- **T012** — RLS helper functions + policies (verbatim PRD 8.4). Passed checker-tests (1st attempt, clean, no findings — the highest-stakes task run so far, constitution items 3 and 4 both explicitly BLOCKER-class for this task). Full detail: all three 8.4 helper functions (`auth_role`, `is_staff`, `my_student_ids`) confirmed byte-verbatim via SHA-256 checksum, not eyeballed; all 14 T009/T010/T011 tables confirmed RLS-enabled with real policies, zero gaps; a full grep sweep of every `create policy` statement confirmed no self-referential same-table subqueries anywhere — every role/ownership check routes through the three security-definer helpers or a cross-table subquery against a *different* table (e.g. `events`→`students`, never `events`→`events`). The `profiles.role::text` cast on `profiles_self_update` was independently proven necessary on a live scratch Postgres (uncast form throws `operator does not exist: role_enum = text`; cast form works and still blocks self role-escalation), with `auth_role()`'s own body confirmed untouched — the cast lives only in the policy. **RLS shape worth remembering for future tasks:** `students` is deliberately scoped to `staff_all` + `own_or_linked_read` only (own row via `id in (select my_student_ids())`); the "teammate name/team for leaderboard" visibility row from the 8.3 matrix (Trap 1) is an intentional, packet-sanctioned gap deferred to **T013's metric/leaderboard views**, not a T012 defect — T013's worker packet should be built with this dependency in mind. `events`/`event_sessions` team-scoping (Trap 2) diverges from the packet's own illustrative snippet: the `team_ids is null` ("all teams" event) check was moved *inside* the cross-table `exists()` against `students` rather than being a standalone top-level `OR`, so any authenticated session with zero rows in `students`/`guardian_links` — including a bare orphan `auth.users` row with no `profiles` row at all — sees **zero** events, rather than leaking every globally-scoped event to profile-less sessions. The checker independently re-reproduced this on a fresh scratch Postgres and gave an explicit reasoned verdict (not a rubber stamp) that this is the correct default-deny security posture per constitution item 4 ("no links = no scope"), even though it also technically denies a real student/parent profile that exists but has zero links yet (an onboarding-lag edge case PRD 8.3 doesn't explicitly address) — logged as a note for any future task that touches onboarding/roster-linkage timing, not a defect to fix now. `teams`/`seasons` (`read_all` authenticated + staff writes) and `guardian_links` (`staff_all` + `own_read`) — both outside the literal 8.3 matrix — were judged reasonable spirit-consistent interpretations, not disputes. T009/T010/T011 confirmed zero diff (constitution item 10); no secrets/service-role keys in the migration; no forbidden-file violations. Evidence included real scratch-Postgres command output (not paraphrased) for anon, orphan-authenticated, student1, parent1, and admin/coach sessions across `students`/`attendance`/`events`/`event_sessions`/`profiles`/`notification_prefs`/`calendar_feeds`, a `profiles` role-escalation block-attempt test, and a no-infinite-recursion confirmation across every role type. See `docs/swarm/verification-log.md` T012 entry and `docs/swarm/archive/T012-worker-packet.md` / `docs/swarm/archive/T012-checker-packet.md` for full packets.

## Active
Nothing currently in flight.

T006, T014, T017, T032, and T061 are Ready and undispatched.

## Known Decisions
- Stack locked: Vite + **React 19** + TS strict + Supabase; Astryx only, no Tailwind/shadcn (PRD D2/D3, constitution item 8). **React 19 is a human-authorized deviation from PRD D2's "React 18"** — see `dispute-log.md` D002 (2026-07-16): `@astryxdesign/core` requires React 19 at runtime (calls `use()`), confirmed by the fact that T002's own shipped `Theme.js` would throw under React 18 at first render. **D002 is now fully closed out end-to-end, not just constitution-amended:** corrective task T002a Passed checker-tests on the merits (2nd check) — react/react-dom actually running at 19.2.7, `npm ls` clean, `react.use()` confirmed callable, a real runtime render of Theme+App passing under vitest+jsdom, and no regression to `volt.ts`'s verbatim DES-03 content. T003 was unblocked accordingly (Blocked→Ready, 2026-07-16).
- RLS default-deny on every table; policies/metric SQL copied verbatim from PRD 8.4, never re-derived or duplicated in TS (constitution items 3/4).
- Astryx props sourced only from `docs/swarm/astryx-api.md`; CLI is a cross-check, not a source (constitution item 2).
- Motivation ethics (constitution item 17 / PRD 5.7 BEH-01…09): honest progress signals only — no streaks, loss-aversion, FOMO, or guilt copy. Folded into task acceptance criteria wherever progress bars, milestones, or meeting history appear (not a separate epic).
- Human gates live in the ledger as checker = "human (George)": T052 (production email), T063 (MIG-04 validation sign-off), T065 (MIG-06 cutover), T070 (Vercel domain go-live).
- **D001 (resolved)** — T001's first checker-tests run FAILed on a git-commit-bundling evidence trap: it inferred worker authorship of `docs/swarm/**` changes from a bundled orchestrator commit, but git identity here doesn't distinguish per-agent authorship (commits mix orchestrator, foreman-authored packets, and hook-generated log lines). Boss-arbiter vacated the verdict. **Standing rule for all future checker packets:** never use git history/commit diffs as evidence of which agent touched a file. Instead compare the task's Allowed Files list against the current file tree state directly. This risk recurs on every checker-tests run unless checker packets keep stating the file-tree-comparison method explicitly.
- **Ledger status-propagation gap (found + fixed 2026-07-16):** when T001 passed, only its direct dependent T002 was flipped Blocked→Ready; T005 and T009 (both dep solely on T001) were incorrectly left Blocked. A full-ledger sweep was run comparing every Blocked task's Deps column against current dependency Status; only tasks with ALL deps Passed flip to Ready. Result: T005 and T009 corrected to Ready (see task-ledger.md). No other rows qualified — every other Blocked task has at least one dependency that is itself not yet Passed. **Standing rule:** on every task Passed close-out, sweep the full ledger for newly-unblocked tasks, not just the task's immediate/listed dependents.
- **T009 PASS full-ledger sweep (2026-07-16):** on T009's PASS-with-MINOR close-out, swept every Blocked row's Deps column against current Status, not just T009's listed direct dependents (T010, T012, T017, T019, T022, T029, T061). Only T010 (deps = T009 alone) had every dependency Passed, so only T010 flipped Blocked→Ready. All other T009-dependent rows still have at least one other unmet dependency (e.g. T012 also needs T010/T011; T019 also needs T017) and correctly remain Blocked. T009's MINOR `avatar_url` finding was routed as an amendment to T019's existing acceptance criteria rather than a new task, since T019 already performs the exact `profiles` INSERT the finding affects.
- **T002a PASS full-ledger sweep (2026-07-16):** on T002a's PASS close-out, per D002's explicit directive T003 (Deps: T002, T002a — both now Passed) flipped Blocked→Ready. Swept every other currently-Blocked row's Deps column against current Status (Passed set at time of sweep: T001, T002, T002a, T009; Ready-but-not-Passed set: T003, T005, T010): T004 (needs T001,T002,T003) stays Blocked — T003 is Ready, not yet Passed. T006 (needs T002,T005) stays Blocked — T005 is Ready, not yet Passed. T011 (needs T010) stays Blocked — T010 is Ready, not yet Passed. Every remaining Blocked row (T007–T008, T012–T070 excluding gates) depends transitively on at least one task that is not yet Passed. No row other than T003 qualified for a status change this sweep.
- **T010 PASS full-ledger sweep (2026-07-17):** on T010's PASS-with-MINOR close-out (performed directly by the orchestrating session per the session-limit note above), swept every Blocked row's Deps against current Status (Passed set: T001, T002, T002a, T009, T010). Only T011 (deps = T010 alone) had every dependency Passed, so only T011 flipped Blocked→Ready. T012 (needs T009,T010,T011) stays Blocked — T011 is Ready, not yet Passed. Every other row still depends transitively on at least one not-yet-Passed task. T010's MINOR `event_sessions.notes` finding routed as amendments to T031 and T039 (whichever lands first should do the small additive migration, not both).
- **T003 PASS full-ledger sweep (2026-07-17):** on T003's clean 1st-attempt PASS close-out, swept every Blocked row's Deps against current Status (Passed set: T001, T002, T002a, T003, T009, T010). Only T004 (deps T001,T002,T003 — all now Passed) flipped Blocked→Ready. T006 (deps T002,T005) stays Blocked — T005 is In Progress, not yet Passed. T060 (deps T003,T011,T046) stays Blocked — T011 and T046 not yet Passed. Every other Blocked row still depends transitively on at least one not-yet-Passed task. No findings on T003 to route as follow-ups.
- **T005 PASS full-ledger sweep (2026-07-17):** on T005's PASS close-out (2nd check, targeted re-check of the kiosk-route fix), swept every Blocked row's Deps against current Status (Passed set: T001, T002, T002a, T003, T005, T009, T010). Only T006 (deps T002,T005 — both now Passed) flipped Blocked→Ready. Every other Blocked row was checked and still depends transitively on at least one not-yet-Passed task (most immediately, T011 which is In Progress but not yet Passed — this alone keeps T012, T060, T061 blocked; T007/T008 stay Blocked pending T006 itself reaching Passed, not just Ready). Full table swept, not just T006's row. T005's two deferred findings (K2 Role-union reconciliation, K3 toast-during-render) were logged as context for T006 in the Completed section above, not spun into new ledger rows.
- **T011 PASS full-ledger sweep (2026-07-17):** on T011's clean 1st-attempt PASS-with-MINOR close-out, swept every Blocked row's Deps against current Status (Passed set: T001, T002, T002a, T003, T005, T009, T010, T011). Two rows qualified: **T012** (deps T009,T010,T011 — all now Passed) flipped Blocked→Ready, and **T061** (deps T009,T010,T011 — all now Passed) flipped Blocked→Ready. Every other Blocked row still depends transitively on at least one not-yet-Passed task — e.g. T013 (needs T012, which is Ready not Passed), T015/T017/T019 (need T012), T048/T051 (need T017/T049/T050), T060 (needs T011 ✓ and T003 ✓ but also T046, not yet Passed). T004 and T006 were already Ready from prior sweeps and remain so. Full table swept row by row, not just T011's listed direct dependents.
- **T004 PASS full-ledger sweep (2026-07-17):** on T004's clean 1st-attempt PASS close-out, swept every Blocked row's Deps against current Status (Passed set: T001, T002, T002a, T003, T004, T005, T009, T010, T011). T004 does not appear in any other task's Deps column anywhere in the ledger, so nothing newly unblocked as a direct result of this PASS — verified by scanning the full Deps column, not assumed. Every currently-Blocked row still depends transitively on at least one not-yet-Passed task (most commonly T012, which is Ready but not yet Passed, or T006, likewise Ready but not yet Passed). T006, T012, and T061 remain the full set of Ready-and-undispatched tasks after this sweep.
- **T012 PASS full-ledger sweep (2026-07-17):** on T012's clean 1st-attempt PASS close-out, swept every Blocked row's Deps against current Status (Passed set: T001, T002, T002a, T003, T004, T005, T009, T010, T011, T012). Four rows qualified, verified by checking each one's FULL Deps list, not just that T012 appeared in it: **T013** (deps T010,T012 — both Passed) flipped Blocked→Ready; **T015** (deps T012 alone — Passed) flipped Blocked→Ready; **T017** (deps T009,T012 — both Passed) flipped Blocked→Ready; **T032** (deps T010,T012 — both Passed) flipped Blocked→Ready — this last one is easy to miss since it's not adjacent to the other three in the ledger (it's an E5 task, `checkin` Edge Function) but its Deps column is exactly T010,T012 and both are now Passed. Checked and confirmed still correctly Blocked (at least one unmet dependency each): T014 (needs T013, Ready not Passed), T016 (needs T006 Ready-not-Passed and T015 now-Ready-not-Passed), T018/T019/T020 (need T017/T018, not yet Passed), T021–T029 (need T019 or T021, not yet Passed), T030/T031 (need T019/T030), T033–T036 (need T032/T031/T033, T032 itself now Ready but not yet Passed), T037 (needs T013), T038–T044 (need T019/T038 etc.), T045–T047 (need T030/T038/T045), T048–T052 (need T017, now Ready not Passed), T053–T070 (need multiple downstream E9–E11 tasks). T006 and T061 were already Ready from prior sweeps and remain so, unaffected by this sweep. Full table swept row by row, not just the three "obvious candidate" rows named in the close-out request.
- **Checker-packet calibration note — background swarm-process artifacts are not per-task findings (2026-07-17, T004 close-out):** T004's checker-tests run flagged, as a "technical violation" of the "zero net diff outside allowed files" acceptance criterion, that new `docs/swarm/active/*.md` files and a hook-appended `verification-log.md` line existed outside the worker's Allowed Files. This is not a real defect and the checker itself said so explicitly ("does not affect deliverable quality," workflow "ready for use") — but it still logged the observation as a task-level finding, which would recur identically on every remaining task if left uncorrected. Root cause: every checker packet foreman-planner writes is itself a new `docs/swarm/active/*.md` file, and the SubagentStop hook appends a `verification-log.md` line on every worker completion — both are normal, expected byproducts of the swarm's own operation, not artifacts the worker produced or could have avoided, and not evidence to weigh per task. This is a narrower, more precise case than D001 (D001 was about not blaming *workers* for `docs/swarm/**` changes they didn't make via git-history inference; this checker correctly didn't blame the worker, but still surfaced the observation as a per-task finding needing a verdict-level mention). **Standing rule for all future checker packets:** the "Forbidden Modification Check" / zero-net-diff instructions must explicitly state that new `docs/swarm/active/*.md` files (worker/checker packets themselves) and `verification-log.md`'s hook-appended completion lines are always-expected background artifacts of swarm operation — checkers should note their presence is normal and not file them as a per-task MINOR/finding at all, the same way they already don't blame the worker for them. No follow-up task created; this is a packet-writing calibration point only.
- **audit_log.actor NOT NULL — centralized operational note, not a per-task amendment (2026-07-17):** T011's checker adjudicated `audit_log.actor uuid not null` (no default, following the established fk-not-marked-null convention) as an acceptable design, but flagged that any future write to `attendance`, `profiles`, `students`, `event_sessions`, or `invites` made *outside* a user's own authenticated browser session (e.g. a service-role Edge Function or a background/cron job) will hard-fail the entire triggering UPDATE unless `SET LOCAL app.actor_id = '<uuid>'` is issued first in that transaction, since `auth.uid()` resolves to null under a service-role connection and the trigger's `coalesce(auth.uid(), nullif(current_setting('app.actor_id', true), '')::uuid)` fallback then has nothing to fall back to. Decision: **log this centrally here rather than amend acceptance criteria onto T051, T036, or T042 specifically.** Reasoning: (1) per the current task specs, none of those three obviously need it yet — T051 (`send-reminders`) only inserts into `email_log`, which carries no audit trigger; T036 (End meeting) and T042 (Mark day complete) primarily perform attendance INSERTs and coach-initiated UPDATEs that, if implemented as ordinary client-side Supabase calls under the coach's own session, will have `auth.uid()` resolve normally with no special handling required; (2) whether any of them actually need `SET LOCAL app.actor_id` depends on implementation choices not yet made (e.g. whether a task's bulk/backfill writes are done via ordinary client calls vs. an RPC or Edge Function running under service-role credentials) — amending acceptance criteria now, before that choice is made, risks being wrong or incomplete; (3) the actual set of at-risk tasks is broader than three rows and not fully knowable yet — e.g. **T032** (`checkin` Edge Function) is a plausible additional candidate since QR check-in Edge Functions typically run under service-role to bypass RLS, and MTG-09's idempotent-duplicate-check-in handling could in principle hit the attendance-post-completion trigger. **Standing rule:** whenever foreman-planner builds a worker packet for any task that writes to `attendance`, `profiles`, `students`, `event_sessions`, or `invites` via a non-interactive/service-role/background context (candidates observed so far: T032, T036, T042, T051, and any future automated job), include an explicit acceptance note that the task must `SET LOCAL app.actor_id = '<acting-or-system-profile-id>'` before such writes, or document why the write path is guaranteed to run under a real user session instead. This is tracked here as a standing decision/risk, not spun into new ledger rows, until a concrete task's implementation choice actually triggers the need.

- **T015 PASS full-ledger sweep (2026-07-17):** on T015's clean 1st-attempt PASS-with-NIT close-out, swept the full Deps column for every currently-Blocked row. Only **T016** (`/login` screen) lists T015 as a dependency (deps: T006,T015); T006 is Ready but not yet Passed, so T016 correctly stays Blocked. No other row in the ledger references T015 anywhere in its Deps column. Nothing newly unblocked as a direct result of this PASS.
- **T013 PASS full-ledger sweep (2026-07-17):** on T013's clean 1st-attempt PASS close-out, swept the full Deps column for every currently-Blocked row. Only **T014** (deps: T013 alone) had every dependency Passed, so it flipped Blocked→Ready. Checked and confirmed still correctly Blocked (at least one unmet dependency each): T037 (needs T030,T013 — T030 not yet Passed), T044 (needs T038,T013), T050 (needs T048,T013), T053/T054/T055 (need T030/T032/T038/T037,T013), T056 (needs T013,T014 — T014 itself now Ready but not yet Passed), T057 (needs T056,T013). No other row references T013 anywhere in its Deps column.

- **CI break on real GitHub Actions infra, fixed 2026-07-17 (post-T004, direct correction, human-reported):** the first live run of T004's shipped `.github/workflows/ci.yml` failed on GitHub's `ubuntu-latest` runner at the "Unit tests" step with `Cannot find native binding ... Cannot find module '@rolldown/binding-linux-x64-gnu'`. This looked like the well-known npm optional-dependencies bug (npm/cli#4828) but the actual root cause was structural: `vitest@4.1.10` (what `package.json`'s `"vitest": "^4.1.10"` resolved to, installed during T002a) requires `vite: ^6.0.0 || ^7.0.0 || ^8.0.0` as a hard dependency — it does **not** support Vite 5. Since the project's top-level `vite` is pinned `^5.4.11` (D2 stack lock) and doesn't satisfy that range, npm silently installed a second, non-deduped `vitest/node_modules/vite@8.1.5` just for vitest's internal use. Vite 8 uses Rolldown (Rust bundler) as its engine, which ships platform-native binaries as optional dependencies — that's where the failing `@rolldown/binding-linux-x64-gnu` package came from. It installed fine in this sandbox (same linux-x64 platform, `npm ci` succeeded locally both before and after diagnosis) but failed on GitHub's runner, consistent with npm's optional-dependency install bug being non-deterministic across npm/Node version pairings rather than platform-specific. **Fix:** downgraded `vitest` to `^3.2.7` (last major supporting `vite: ^5.0.0 || ^6.0.0 || ^7.0.0-0` — compatible with the project's actual Vite 5), clean `rm -rf node_modules package-lock.json && npm install`. Confirmed the nested nondeduped `vite@8`/rolldown-binding dependency is gone entirely (`npm ls vite vitest` now shows a single deduped `vite@5.4.21` used everywhere, no `@rolldown/binding-*` packages anywhere in the lockfile); typecheck/lint/format:check/test/build all still pass, build output byte-identical to before (same `dist/assets/index-DMHTuYRB.js` size), so T004's CI bundle-size gate behavior is unaffected. This is a correction to already-Passed work (T002a introduced the `vitest ^4.1.10` pin; T004's CI workflow is what surfaced the bug once it ran on real infrastructure — both agents had explicitly flagged that a live GitHub Actions run couldn't be proven from this sandbox, and this is that exact flagged gap materializing). Neither T002a's nor T004's PASS verdict is invalidated — their local verification was accurate for what it tested. **Standing rule:** whenever a task pins a new major version of a dev-tooling dependency with its own peer/hard requirement on another already-locked dependency (e.g. a test runner's own Vite requirement vs. the app's pinned Vite), the worker packet should require checking that dependency's actual declared range against the locked version, not just confirming `npm install` succeeds — a nested, non-deduped second copy of a core build tool is itself a signal worth surfacing even when everything still builds locally.
- **CI break #2 (follow-on, same investigation), fixed 2026-07-17:** after the vitest downgrade above, the *next* real CI run on GitHub failed with a different error at the same "Unit tests" step: `ERR_REQUIRE_ESM` from `html-encoding-sniffer` (a jsdom dependency) `require()`-ing the ESM-only `@exodus/bytes` package. Root cause confirmed directly from the CI log's own `npm warn EBADENGINE` output (not speculation): `whatwg-url@16.0.1` (another jsdom transitive dependency) declares `engines.node = "^20.19.0 || ^22.12.0 || >=24.0.0"`. T004's workflow pinned Node `20.18.1`, just below that floor — Node's synchronous `require(esm)` interop was stabilized starting at exactly those versions (20.19.0 on the 20.x line), so 20.18.1 throws where 20.19.0+ doesn't. **Fix:** bumped the workflow's pinned Node version to `22.22.2` (verified as a real published Node release, not just this sandbox's build; matches the Node version this whole project has been developed/tested against throughout, satisfies the `^22.12.0` branch with margin, and sidesteps GitHub's own Node 20 deprecation warnings). Verified locally (typecheck/lint/format:check/test/build all pass, unchanged build output) and **confirmed on real GitHub Actions infrastructure**: both CI runs at commit `5655908` (push + pull_request triggers) completed `success`. PR #1 has zero unresolved review comments as of this check. This closes out both CI-break corrections; T004's workflow is now verified working end-to-end on real infra, not just locally.

## Current Risks
- **Session-limit pause (resolved, 2026-07-17):** background agent dispatches earlier failed with "You've hit your session limit" (reset 03:30 UTC); see the "Session-limit interruption" note above for what happened during that window. Dispatch capacity has since resumed — T003's checker-tests run, T005's checker-reviewer run (both attempts), and T011's checker-tests run have all since completed cleanly. Nothing currently in flight. No open risk from this item at present.
- **audit_log.actor NOT NULL — standing operational risk for future service-role/background writes:** see the dedicated Known Decisions bullet above ("audit_log.actor NOT NULL — centralized operational note"). No action needed until a task with a genuinely non-interactive write path to one of the five trigger-guarded tables is actually dispatched; flagged here so it isn't lost.
- **External prerequisites (not swarm tasks, block specific ledger tasks):** (1) George must create the new Supabase project (blocks T009 onward for live verification); (2) George must create the Google OAuth client (blocks T015 end-to-end); (3) ~~George must verify the `mail.voltfrc.org` Resend sending domain (blocks T052)~~ — **done, confirmed 2026-07-17.** `mail.voltfrc.org` shows Verified in Resend (DKIM TXT record verified, SPF/MX records verified, Enable Sending on, domain-verified event logged Jul 15 9:33 PM). This does not itself unblock T052 in the ledger — T052 (deps: T051) is still Blocked behind the full E8 email chain (T048 Resend integration → T049 templates → T050 digest → T051 send-reminders, none built yet); this just clears the external prerequisite so T052 won't be stuck waiting on George once that chain is reached. (4) George must add the Vercel CNAME for `portal.voltfrc.org` (blocks T070). The swarm cannot start real production email or domain go-live work without the remaining unresolved prerequisites (1, 2, 4).
- **T002 upstream type gap (resolved locally, informational only):** `@astryxdesign/core@0.1.6`'s `TypographyRole` interface omits the `url` field its own JSDoc documents. Worked around via `src/theme/astryx-augment.d.ts` (verified correct, T002 Passed). Not a stack-lock or spec problem. NIT follow-up: consider filing an upstream issue against `@astryxdesign/core@0.1.6` for this gap — log only, no swarm action needed; the local augmentation file already covers it. Detail archived at `docs/swarm/archive/T002-latest-failure.md`.
- Loop limit is 3 failed attempts per task before mandatory escalation to boss-arbiter (constitution "Loop Limit").
- No task may be marked complete on worker self-report; every row requires checker-inspected evidence per the Definition of Done.

## Next Recommended Task
Six tasks are now Ready. T006/T013/T015/T017/T032/T061 are mutually file-disjoint (different Allowed Files) — safe to dispatch in parallel, subject to the notes below:
1. **T013 (Metric views, verbatim 8.4)** — deps T010,T012 both Passed. High priority: T014 (fixture tests), T037, T044, T050, T056, T057 and several E9 dashboards all wait on T013 next. Also expected to close T012's deliberately-deferred Trap 1 gap (`students` teammate name/team visibility for the leaderboard) — flag this explicitly in the worker packet so it isn't silently dropped. Constitution item 3 BLOCKER-class if the views are re-derived instead of copied verbatim, or if any metric formula gets duplicated in TS.
2. **T015 (Supabase Auth provider config)** — deps T012 alone, Passed. Note: external blocker still applies — George's Google OAuth client credential doesn't yet exist, so this task can be authored/staged but not verified end-to-end until that lands (see Current Risks below).
3. **T017 (`send-invite` Edge Function)** — deps T009,T012 both Passed. High priority: T018, T019, T020, T024, T027, T048 all wait on T017 next. Constitution item 5 BLOCKER-class if a service-role key ends up in the frontend bundle — flag explicitly.
4. **T032 (`checkin` Edge Function, HMAC rotating token)** — deps T010,T012 both Passed. Independent of the E3 auth chain; can run in parallel. Constitution item 5 BLOCKER-class (secret must never reach the frontend bundle) applies here too.
5. **T061 (Schema verification + mapping doc copy, MIG-01/02)** — deps T009,T010,T011 all Passed; still Ready from the prior sweep, not yet dispatched. Note for the worker packet: MIG-01 requires introspecting the *live old project*, which may not yet be accessible — confirm access before dispatch or scope this attempt to MIG-02 (verbatim copy of 10.2) if the old project isn't reachable yet, flagging the gap rather than guessing.
6. **T006 (AppShell + TopNav)** — deps T002, T005 both Passed; still Ready from an earlier sweep, not yet dispatched. Worth flagging to the T006 worker/checker: K2 (Role union completeness) and K3 (toast-during-render in RequireRole) are logged context from T005 they may want to address while touching this code.
7. After T006 closes out Passed, T007 (SideNav) becomes a live candidate (deps T006). After T013 closes out Passed, T014/T037/T044/T050 etc. open up. After T017 closes out Passed, T018/T019/T048 open up (T019 also needs T017 specifically, not just T012+T009). After T032 closes out Passed, T033/T034/T035 open up (T033 also needs T031, not yet Ready).
8. T004 and T012 are now Passed — no longer dispatch candidates.
